# redis-nuker
redis nuker is an sh script that deletes the keys, changes the working directory where redis saves its data files, changes the name of the database snapshot file, forces Redis to take everything currently in memory and writes it to the disk immediately.
